# Network Packet Capture

Capture the network traffic of the host running this `Rust` binary.

## Be Advised
The binary must be executed with `sudo` privledges to get access to the network interfaces.
